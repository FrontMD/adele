-
	const defaultSSocialsProps = {
		classes: '',
	}

mixin news-socials(props = defaultSSocialsProps)
	-
		const checkProps = {
			classes: props.classes ? props.classes : defaultSSocialsProps.classes,
		}

	section.news-socials(class=checkProps.classes)&attributes(attributes)
		.m-container
			.section-flex
				block